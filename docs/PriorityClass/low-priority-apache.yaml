apiVersion: apps/v1
kind: Deployment
metadata:
  name: php-apache-low
spec:
  replicas: 4
  selector:
    matchLabels:
      run: php-apache-low
  template:
    metadata:
      labels:
        run: php-apache-low
    spec:
      priorityClassName: low-priority
      containers:
      - name: php-apache
        image: registry.k8s.io/hpa-example
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: 500m
          requests:
            cpu: 200m
---
apiVersion: v1
kind: Service
metadata:
  name: php-apache-low
  labels:
    run: php-apache-low
spec:
  ports:
  - port: 80
  selector:
    run: php-apache-low
